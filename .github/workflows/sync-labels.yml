name: Sync Labels
on:
  workflow_call:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: EndBug/label-sync@v2
        with:
          config-file: 'https://raw.githubusercontent.comturbot/steampipe-workflows/blob/main/.github/labels.yml'
          delete-other-labels: false
          dry-run: true
